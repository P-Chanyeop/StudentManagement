spring:
  application:
    name: StudentManagement
  profiles:
    active: dev
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
    show-sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

# JWT 설정
jwt:
  secret: ${JWT_SECRET:your-secret-key-change-this-in-production-minimum-256-bits-required-for-hs256-algorithm}
  expiration: 86400000  # 24시간 (밀리초)
  refresh-expiration: 604800000  # 7일 (밀리초)

# SMS API 설정
sms:
  provider: ${SMS_PROVIDER:aligo}
  aligo:
    api-key: ${ALIGO_API_KEY:3r91ijgybd265j3x9nwtmwoe1cly8r7l}
    user-id: ${ALIGO_USER_ID:qkrcksduq1gh}
    sender: ${ALIGO_SENDER:01044147579}
    enabled: ${ALIGO_ENABLED:true}

# 공휴일 API 설정 (공공데이터포털)
holiday:
  api:
    key: ${HOLIDAY_API_KEY:89f7714a095eef41a5d331112f94b5ac301f9bab83eaf354e9327faf1f2fec74}

# 네이버 예약 크롤링 설정
naver:
  booking:
    url: ${NAVER_BOOKING_URL:https://booking.naver.com/your-booking-page}
    cron: "0 0 2 * * ?"  # 매일 새벽 2시에 실행

# 파일 업로드 설정
file:
  upload-dir: ${FILE_UPLOAD_DIR:uploads}

# 포트원(PortOne) 결제 설정
portone:
  api-key: ${PORTONE_API_KEY:}
  api-secret: ${PORTONE_API_SECRET:}
  imp-code: ${PORTONE_IMP_CODE:}  # 가맹점 식별코드
  pg-provider: ${PORTONE_PG_PROVIDER:nice}  # 기본 PG사 (nice, kcp, inicis, kakaopay 등)
  api-url: ${PORTONE_API_URL:https://api.iamport.kr}
  test-mode: ${PORTONE_TEST_MODE:true}  # 테스트 모드

---
# 개발 환경 (H2 Database)
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:mysql://ls-b8620b4ccbdc824c0cb2bb974b1b68d676f10035.c1wq6m02cidt.ap-northeast-2.rds.amazonaws.com:3306/dbmaster?useSSL=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dbmasteruser
    password: XzW#p?NF)_R#iJP&?r1?wZ7)A4OZ#VW2
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

logging:
  level:
    web.kplay.studentmanagement: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

---
# 운영 환경 (MySQL)
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:student_management}?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    web.kplay.studentmanagement: INFO
    org.hibernate.SQL: INFO
